{% style %}
  bq-cart-totals {
    --book-button-button-color: var(--color-accent-background);
    --secondary-button-button-border: 1px solid var(--color-accent-background);
     --book-button-button-border-radius: 0;
  }
  bq-cart-lines {
    --line-control-border-radius: 0;
    --line-image-border-radius: 0;
  }
{% endstyle %}
